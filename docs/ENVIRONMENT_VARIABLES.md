# Environment variables

| Variable | Scope | Required | Purpose |
|---|---|---|---|
| `NEXT_PUBLIC_SITE_URL` | client-visible build configuration | production | Canonical metadata, robots, and sitemap origin. Use `https://dionismarkov.com`. |

No secret variables are currently required. Public-prefixed variables must never contain secrets.
